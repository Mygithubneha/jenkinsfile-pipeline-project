pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "My first jenkinsfile"
                sh 'python --version'
                sh 'python pipeline.py'
                
                
            }
        }
    }
}